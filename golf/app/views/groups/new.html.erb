<h2>New Group for <%= @current_user.name %></h2>

<%= form_for @group do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :num_players %>
  <%= f.text_field :num_players %>

  <%= f.submit %>
<% end %>
